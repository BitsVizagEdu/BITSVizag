export let items = ['About-ABWS', 'About-BITS-Vizag', 'Message-from-Secretary & Correspondent', 'Message-from-Principal']

export function replaceHyphenWithSpace(text) {
    return text.replace(/-/g, " ")
}