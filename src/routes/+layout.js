export const prerender = true;

// Preload all JS modules for linked pages when the user hovers a link
// (pairs with data-sveltekit-preload-data="hover" in app.html)
export const trailingSlash = 'never';
